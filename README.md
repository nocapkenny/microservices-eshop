# Django Microservices Shop – Интернет-магазин на микросервисах

## 📃 Содержание

- [❗ Немного о проекте](#-немного-о-проекте)
- [💻 Технологический стек](#-технологический-стек)
- [🏗️ Архитектура](#-архитектура)
- [📡 API Endpoints](#-api-endpoints)
- [✈️ Запуск проекта](#️-запуск-проекта)
- [🧩 Фронтенд](#-фронтенд)

---

## ❗ Немного о проекте

Многофункциональный интернет-магазин, построенный по принципам **микросервисной архитектуры** с использованием **Django REST Framework** и **JWT-аутентификации**.

### 🔑 Ключевые особенности:

- **Микросервисы** — каждый домен (пользователи, товары, корзина, заказы) выделен в отдельный независимый сервис
- **RESTful API** — чистые, документированные и согласованные эндпоинты
- **JWT-аутентификация** — безопасный и stateless доступ к защищённым ресурсам
- **CORS-поддержка** — готов к работе с фронтендом на другом домене/порту
- **Redis** — используется для кэширования и взаимодействия между сервисами
- **Vue.js + Vite** — современный, быстрый и реактивный фронтенд

### ❓ Возможности:

- **Пользователи**: регистрация, вход, профиль, JWT-токены
- **Товары**: каталог, фильтрация по категориям
- **Корзина**: добавление, изменение количества, удаление, очистка
- **Заказы**: создание заказа на основе корзины, просмотр истории

---

## 💻 Технологический стек

### Бэкенд:

- **Python 3.9+**
- **Django 5.2+** + **Django REST Framework**
- **djangorestframework-simplejwt** — для JWT-аутентификации
- **Celery + Redis** — асинхронные задачи
- SQLite — основная база данных
- **django-cors-headers** — поддержка CORS

### Фронтенд:

- **Vue 3** + **Composition API**
- **Vite** — быстрая сборка
- **SCSS** — стилизация с переменными и вложенностью
- **Pinia** — управление состоянием (корзина, пользователь)
- **Axios** — HTTP-запросы с поддержкой JWT

## 🏗️ Архитектура


| Сервис           | Порт | Описание                                                        |
| ---------------------- | -------- | ----------------------------------------------------------------------- |
| 🌐**API Gateway**      | 8000     | Единая точка входа для фронтенда            |
| 👥**Users Service**    | 8004     | Регистрация, авторизация, профиль          |
| 📦**Products Service** | 8001     | Каталог товаров, категории, фильтрация |
| 👜**Cart Service**     | 8002     | Управление корзиной пользователя          |
| 🛒**Order Service**    | 8003     | Создание и отслеживание заказов             |                                                                    
| 🧠**Redis**            | 6379     | Кэширование, Celery, сессии                            |

> 💡 В текущей реализации все сервисы используют собственные базы данных

---

## 📡 API Endpoints

### 🔐 Аутентификация и пользователи (`:8004`)

- `POST /api/auth/register/` — регистрация
- `POST /api/auth/login/` — логин
- `GET /api/users/profile/` — профиль текущего пользователя

### 📦 Товары (`:8001`)

- `GET /api/products/` — список товаров (с пагинацией и фильтрацией)
- `GET /api/categories/` — список категорий
- `GET /api/products/{slug}/` — список товаров по категории

### 👜 Корзина (`:8002`)

- `GET /api/cart/` — получить корзину
- `POST /api/cart/add/` — добавить товар
- `PATCH /api/cart/update/{id}/` — изменить количество товаров
- `DELETE /api/cart/delete/{id}/` — удалить товар
- `POST /api/cart/clear/` — очистить корзину

### 🛒 Заказы (`:8003`)

- `POST /api/order/create/` — создать заказ из корзины
- `GET /api/order/list/` — список заказов пользователя

> Все эндпоинты (кроме регистрации/логина и) требуют **авторизации через JWT** в заголовке:
> `Authorization: Bearer <access_token>`

---

## ✈️ Запуск проекта

### 📌 Предварительные требования

- Python 3.9+
- PostgreSQL (или SQLite для разработки)
- Redis (для полной функциональности)
- Git

### 🔧 Запуск вручную (по терминалам)

```bash
# Терминал 1 — Сервис пользователей
cd services/user-service
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver 0.0.0.0:8004
```

```bash
# Терминал 2 — Сервис товаров
cd services/product-service
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver 0.0.0.0:8001
```

```bash
# Терминал 3 — Сервис корзины
cd services/cart-service
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver 0.0.0.0:8002
```

```bash
# Терминал 4 — Сервис заказов
cd services/order-service
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver 0.0.0.0:8003
```

```bash
# Терминал 5 — API Gateway (опционально)
cd api-gateway
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver 0.0.0.0:8000
```

> 💡 API Gateway пока не обязателен — фронтенд может обращаться напрямую к сервисам.

### 🐳 Запуск через Docker (в разработке)

> Поддержка Docker будет добавлена в ближайшее время.

---

## 🧩 Фронтенд

Фронтенд реализован на **Vue 3 + Vite** и состоит из:

- Страницы каталога с карточками товаров
- Страницы деталей товара
- Боковой панели корзины (`<AsideCart>`)
- Модальных окон авторизации
- Реактивного управления состоянием через **Pinia**

### Основные фичи:

- Добавление в корзину без перезагрузки
- Обновление количества с мгновенным откликом
- Блокировка кнопок при недоступности товара
- Защита от дублирования товара в корзине
- Адаптивный дизайн в тёмной теме

---

> ✨ Проект активно развивается. В планах:
>
> - Интеграция Celery для асинхронной отправки email
> - Разделение баз данных по сервисам
> - Добавление Docker Compose
> - Поддержка размеров и характеристик товара

---
